"""FastAPI application entry point."""

# TODO: Create FastAPI app instance, mount all routers from rigor/api/routes/
#   under the /api/v1 prefix (runs, datasets, models, prompts, health).
#   Call db create_all() on startup via a lifespan context manager.
