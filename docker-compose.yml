services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: rigor
      POSTGRES_PASSWORD: rigor
      POSTGRES_DB: rigor
    volumes:
      - rigor_postgres_data:/var/lib/postgresql/data

volumes:
  rigor_postgres_data:
