"""Tests for built-in metrics."""

# TODO: Add unit tests for exact_match and pass_at_1:
#   - exact_match: matching strings, mismatched strings, whitespace trimming
#   - pass_at_1: valid Python that passes tests, syntax error, infinite loop (timeout), failing assertion
